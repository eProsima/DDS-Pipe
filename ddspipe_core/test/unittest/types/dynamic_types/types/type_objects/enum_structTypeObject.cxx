// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file enum_structTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace { char dummy; }
#endif

#include "enum_struct.h"
#include "enum_structTypeObject.h"
#include <mutex>
#include <utility>
#include <sstream>
#include <fastdds/rtps/common/CdrSerialization.hpp>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>

using namespace eprosima::fastrtps::rtps;

void registerenum_structTypes()
{
    static std::once_flag once_flag;
    std::call_once(once_flag, []()
            {
                TypeObjectFactory *factory = TypeObjectFactory::get_instance();
                factory->add_type_object("ColorEnum", GetColorEnumIdentifier(true),
                GetColorEnumObject(true));
                factory->add_type_object("ColorEnum", GetColorEnumIdentifier(false),
                GetColorEnumObject(false));


                factory->add_type_object("enum_struct", Getenum_structIdentifier(true),
                Getenum_structObject(true));
                factory->add_type_object("enum_struct", Getenum_structIdentifier(false),
                Getenum_structObject(false));

            });
}

const TypeIdentifier* GetColorEnumIdentifier(bool complete)
{
    const TypeIdentifier* c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("ColorEnum", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetColorEnumObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("ColorEnum", complete);
}

const TypeObject* GetColorEnumObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ColorEnum", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteColorEnumObject();
    }
    // else
    return GetMinimalColorEnumObject();
}

const TypeObject* GetMinimalColorEnumObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ColorEnum", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_ENUM);

    // No flags apply
    //type_object->minimal().enumerated_type().enum_flags().IS_FINAL(false);
    //type_object->minimal().enumerated_type().enum_flags().IS_APPENDABLE(false);
    //type_object->minimal().enumerated_type().enum_flags().IS_MUTABLE(false);
    //type_object->minimal().enumerated_type().enum_flags().IS_NESTED(false);
    //type_object->minimal().enumerated_type().enum_flags().IS_AUTOID_HASH(false);

    type_object->minimal().enumerated_type().header().common().bit_bound(32); // TODO fixed by IDL, isn't?

    uint32_t value = 0;
    MinimalEnumeratedLiteral mel_RED;
    mel_RED.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    mel_RED.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    mel_RED.common().flags().IS_EXTERNAL(false); // Doesn't apply
    mel_RED.common().flags().IS_OPTIONAL(false); // Doesn't apply
    mel_RED.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    mel_RED.common().flags().IS_KEY(false); // Doesn't apply
    mel_RED.common().flags().IS_DEFAULT(false);
    mel_RED.common().value(value++);
    MD5 RED_hash("RED");
    for(int i = 0; i < 4; ++i)
    {
        mel_RED.detail().name_hash()[i] = RED_hash.digest[i];
    }
    type_object->minimal().enumerated_type().literal_seq().emplace_back(mel_RED);

    MinimalEnumeratedLiteral mel_GREEN;
    mel_GREEN.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    mel_GREEN.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    mel_GREEN.common().flags().IS_EXTERNAL(false); // Doesn't apply
    mel_GREEN.common().flags().IS_OPTIONAL(false); // Doesn't apply
    mel_GREEN.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    mel_GREEN.common().flags().IS_KEY(false); // Doesn't apply
    mel_GREEN.common().flags().IS_DEFAULT(false);
    mel_GREEN.common().value(value++);
    MD5 GREEN_hash("GREEN");
    for(int i = 0; i < 4; ++i)
    {
        mel_GREEN.detail().name_hash()[i] = GREEN_hash.digest[i];
    }
    type_object->minimal().enumerated_type().literal_seq().emplace_back(mel_GREEN);

    MinimalEnumeratedLiteral mel_BLUE;
    mel_BLUE.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    mel_BLUE.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    mel_BLUE.common().flags().IS_EXTERNAL(false); // Doesn't apply
    mel_BLUE.common().flags().IS_OPTIONAL(false); // Doesn't apply
    mel_BLUE.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    mel_BLUE.common().flags().IS_KEY(false); // Doesn't apply
    mel_BLUE.common().flags().IS_DEFAULT(false);
    mel_BLUE.common().value(value++);
    MD5 BLUE_hash("BLUE");
    for(int i = 0; i < 4; ++i)
    {
        mel_BLUE.detail().name_hash()[i] = BLUE_hash.digest[i];
    }
    type_object->minimal().enumerated_type().literal_seq().emplace_back(mel_BLUE);


    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->minimal().enumerated_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("ColorEnum", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("ColorEnum", false);
}

const TypeObject* GetCompleteColorEnumObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("ColorEnum", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_ENUM);

    // No flags apply
    //type_object->complete().enumerated_type().enum_flags().IS_FINAL(false);
    //type_object->complete().enumerated_type().enum_flags().IS_APPENDABLE(false);
    //type_object->complete().enumerated_type().enum_flags().IS_MUTABLE(false);
    //type_object->complete().enumerated_type().enum_flags().IS_NESTED(false);
    //type_object->complete().enumerated_type().enum_flags().IS_AUTOID_HASH(false);

    type_object->complete().enumerated_type().header().common().bit_bound(32); // TODO fixed by IDL, isn't?
    type_object->complete().enumerated_type().header().detail().type_name("ColorEnum");

    uint32_t value = 0;
    CompleteEnumeratedLiteral cel_RED;
    cel_RED.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    cel_RED.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    cel_RED.common().flags().IS_EXTERNAL(false); // Doesn't apply
    cel_RED.common().flags().IS_OPTIONAL(false); // Doesn't apply
    cel_RED.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    cel_RED.common().flags().IS_KEY(false); // Doesn't apply
    cel_RED.common().flags().IS_DEFAULT(false);
    cel_RED.common().value(value++);
    cel_RED.detail().name("RED");

    type_object->complete().enumerated_type().literal_seq().emplace_back(cel_RED);

    CompleteEnumeratedLiteral cel_GREEN;
    cel_GREEN.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    cel_GREEN.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    cel_GREEN.common().flags().IS_EXTERNAL(false); // Doesn't apply
    cel_GREEN.common().flags().IS_OPTIONAL(false); // Doesn't apply
    cel_GREEN.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    cel_GREEN.common().flags().IS_KEY(false); // Doesn't apply
    cel_GREEN.common().flags().IS_DEFAULT(false);
    cel_GREEN.common().value(value++);
    cel_GREEN.detail().name("GREEN");

    type_object->complete().enumerated_type().literal_seq().emplace_back(cel_GREEN);

    CompleteEnumeratedLiteral cel_BLUE;
    cel_BLUE.common().flags().TRY_CONSTRUCT1(false); // Doesn't apply
    cel_BLUE.common().flags().TRY_CONSTRUCT2(false); // Doesn't apply
    cel_BLUE.common().flags().IS_EXTERNAL(false); // Doesn't apply
    cel_BLUE.common().flags().IS_OPTIONAL(false); // Doesn't apply
    cel_BLUE.common().flags().IS_MUST_UNDERSTAND(false); // Doesn't apply
    cel_BLUE.common().flags().IS_KEY(false); // Doesn't apply
    cel_BLUE.common().flags().IS_DEFAULT(false);
    cel_BLUE.common().value(value++);
    cel_BLUE.detail().name("BLUE");

    type_object->complete().enumerated_type().literal_seq().emplace_back(cel_BLUE);


    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->complete().enumerated_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("ColorEnum", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("ColorEnum", true);
}



const TypeIdentifier* Getenum_structIdentifier(bool complete)
{
    const TypeIdentifier * c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("enum_struct", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    Getenum_structObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("enum_struct", complete);
}

const TypeObject* Getenum_structObject(bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("enum_struct", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteenum_structObject();
    }
    //else
    return GetMinimalenum_structObject();
}

const TypeObject* GetMinimalenum_structObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("enum_struct", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_index;
    mst_index.common().member_id(memberId++);
    mst_index.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_index.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_index.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_index.common().member_flags().IS_OPTIONAL(false);
    mst_index.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_index.common().member_flags().IS_KEY(false);
    mst_index.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_index.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    MD5 index_hash("index");
    for(int i = 0; i < 4; ++i)
    {
        mst_index.detail().name_hash()[i] = index_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_index);

    MinimalStructMember mst_enum_value;
    mst_enum_value.common().member_id(memberId++);
    mst_enum_value.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_enum_value.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_enum_value.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_enum_value.common().member_flags().IS_OPTIONAL(false);
    mst_enum_value.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_enum_value.common().member_flags().IS_KEY(false);
    mst_enum_value.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_enum_value.common().member_type_id(*GetColorEnumIdentifier(false));

    MD5 enum_value_hash("enum_value");
    for(int i = 0; i < 4; ++i)
    {
        mst_enum_value.detail().name_hash()[i] = enum_value_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_enum_value);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("enum_struct", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("enum_struct", false);
}

const TypeObject* GetCompleteenum_structObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("enum_struct", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject *type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_index;
    cst_index.common().member_id(memberId++);
    cst_index.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_index.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_index.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_index.common().member_flags().IS_OPTIONAL(false);
    cst_index.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_index.common().member_flags().IS_KEY(false);
    cst_index.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_index.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    cst_index.detail().name("index");

    type_object->complete().struct_type().member_seq().emplace_back(cst_index);

    CompleteStructMember cst_enum_value;
    cst_enum_value.common().member_id(memberId++);
    cst_enum_value.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_enum_value.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_enum_value.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_enum_value.common().member_flags().IS_OPTIONAL(false);
    cst_enum_value.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_enum_value.common().member_flags().IS_KEY(false);
    cst_enum_value.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_enum_value.common().member_type_id(*GetColorEnumIdentifier(true));

    cst_enum_value.detail().name("enum_value");

    type_object->complete().struct_type().member_seq().emplace_back(cst_enum_value);


    // Header
    type_object->complete().struct_type().header().detail().type_name("enum_struct");
    // TODO inheritance
    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
        calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for(int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("enum_struct", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("enum_struct", true);
}
