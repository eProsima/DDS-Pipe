// Copyright 2016 Proyectos y Sistemas de Mantenimiento SL (eProsima).
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     http://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.

/*!
 * @file MonitoringTopicsTypeObject.cpp
 * This source file contains the definition of the described types in the IDL file.
 *
 * This file was generated by the tool fastddsgen.
 */

#ifdef _WIN32
// Remove linker warning LNK4221 on Visual Studio
namespace {
char dummy;
} // namespace
#endif // ifdef _WIN32

#include <ddspipe_core/types/monitoring/topics/v2/MonitoringTopics.h>
#include <ddspipe_core/types/monitoring/topics/v2/MonitoringTopicsTypeObject.h>
#include <mutex>
#include <utility>
#include <sstream>
#include <fastdds/rtps/common/CdrSerialization.hpp>
#include <fastrtps/rtps/common/SerializedPayload.h>
#include <fastrtps/utils/md5.h>
#include <fastrtps/types/TypeObjectFactory.h>
#include <fastrtps/types/TypeNamesGenerator.h>
#include <fastrtps/types/AnnotationParameterValue.h>

using namespace eprosima::fastrtps::rtps;

void registerMonitoringTopicsTypes()
{
    static std::once_flag once_flag;
    std::call_once(once_flag, []()
            {
                TypeObjectFactory* factory = TypeObjectFactory::get_instance();
                factory->add_type_object("DdsTopicData", GetDdsTopicDataIdentifier(true),
                GetDdsTopicDataObject(true));
                factory->add_type_object("DdsTopicData", GetDdsTopicDataIdentifier(false),
                GetDdsTopicDataObject(false));


                factory->add_type_object("DdsTopic", GetDdsTopicIdentifier(true),
                GetDdsTopicObject(true));
                factory->add_type_object("DdsTopic", GetDdsTopicIdentifier(false),
                GetDdsTopicObject(false));


                factory->add_type_object("MonitoringTopics", GetMonitoringTopicsIdentifier(true),
                GetMonitoringTopicsObject(true));
                factory->add_type_object("MonitoringTopics", GetMonitoringTopicsIdentifier(false),
                GetMonitoringTopicsObject(false));

            });
}

const TypeIdentifier* GetDdsTopicDataIdentifier(
        bool complete)
{
    const TypeIdentifier* c_identifier =
            TypeObjectFactory::get_instance()->get_type_identifier("DdsTopicData", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetDdsTopicDataObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("DdsTopicData", complete);
}

const TypeObject* GetDdsTopicDataObject(
        bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopicData", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteDdsTopicDataObject();
    }
    //else
    return GetMinimalDdsTopicDataObject();
}

const TypeObject* GetMinimalDdsTopicDataObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopicData", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_participant_id;
    mst_participant_id.common().member_id(memberId++);
    mst_participant_id.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_participant_id.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_participant_id.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_participant_id.common().member_flags().IS_OPTIONAL(false);
    mst_participant_id.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_participant_id.common().member_flags().IS_KEY(false);
    mst_participant_id.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_participant_id.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    MD5 participant_id_hash("participant_id");
    for (int i = 0; i < 4; ++i)
    {
        mst_participant_id.detail().name_hash()[i] = participant_id_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_participant_id);

    MinimalStructMember mst_msgs_lost;
    mst_msgs_lost.common().member_id(memberId++);
    mst_msgs_lost.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_msgs_lost.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_msgs_lost.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_msgs_lost.common().member_flags().IS_OPTIONAL(false);
    mst_msgs_lost.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_msgs_lost.common().member_flags().IS_KEY(false);
    mst_msgs_lost.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_msgs_lost.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    MD5 msgs_lost_hash("msgs_lost");
    for (int i = 0; i < 4; ++i)
    {
        mst_msgs_lost.detail().name_hash()[i] = msgs_lost_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_msgs_lost);

    MinimalStructMember mst_msgs_received;
    mst_msgs_received.common().member_id(memberId++);
    mst_msgs_received.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_msgs_received.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_msgs_received.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_msgs_received.common().member_flags().IS_OPTIONAL(false);
    mst_msgs_received.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_msgs_received.common().member_flags().IS_KEY(false);
    mst_msgs_received.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_msgs_received.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t",
            false));


    MD5 msgs_received_hash("msgs_received");
    for (int i = 0; i < 4; ++i)
    {
        mst_msgs_received.detail().name_hash()[i] = msgs_received_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_msgs_received);

    MinimalStructMember mst_msg_rx_rate;
    mst_msg_rx_rate.common().member_id(memberId++);
    mst_msg_rx_rate.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_msg_rx_rate.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_msg_rx_rate.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_msg_rx_rate.common().member_flags().IS_OPTIONAL(false);
    mst_msg_rx_rate.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_msg_rx_rate.common().member_flags().IS_KEY(false);
    mst_msg_rx_rate.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_msg_rx_rate.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("double",
            false));


    MD5 msg_rx_rate_hash("msg_rx_rate");
    for (int i = 0; i < 4; ++i)
    {
        mst_msg_rx_rate.detail().name_hash()[i] = msg_rx_rate_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_msg_rx_rate);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("DdsTopicData", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("DdsTopicData", false);
}

const TypeObject* GetCompleteDdsTopicDataObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopicData", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_participant_id;
    cst_participant_id.common().member_id(memberId++);
    cst_participant_id.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_participant_id.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_participant_id.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_participant_id.common().member_flags().IS_OPTIONAL(false);
    cst_participant_id.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_participant_id.common().member_flags().IS_KEY(false);
    cst_participant_id.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_participant_id.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    cst_participant_id.detail().name("participant_id");

    type_object->complete().struct_type().member_seq().emplace_back(cst_participant_id);

    CompleteStructMember cst_msgs_lost;
    cst_msgs_lost.common().member_id(memberId++);
    cst_msgs_lost.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_msgs_lost.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_msgs_lost.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_msgs_lost.common().member_flags().IS_OPTIONAL(false);
    cst_msgs_lost.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_msgs_lost.common().member_flags().IS_KEY(false);
    cst_msgs_lost.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_msgs_lost.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t", false));


    cst_msgs_lost.detail().name("msgs_lost");

    type_object->complete().struct_type().member_seq().emplace_back(cst_msgs_lost);

    CompleteStructMember cst_msgs_received;
    cst_msgs_received.common().member_id(memberId++);
    cst_msgs_received.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_msgs_received.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_msgs_received.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_msgs_received.common().member_flags().IS_OPTIONAL(false);
    cst_msgs_received.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_msgs_received.common().member_flags().IS_KEY(false);
    cst_msgs_received.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_msgs_received.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("uint32_t",
            false));


    cst_msgs_received.detail().name("msgs_received");

    type_object->complete().struct_type().member_seq().emplace_back(cst_msgs_received);

    CompleteStructMember cst_msg_rx_rate;
    cst_msg_rx_rate.common().member_id(memberId++);
    cst_msg_rx_rate.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_msg_rx_rate.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_msg_rx_rate.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_msg_rx_rate.common().member_flags().IS_OPTIONAL(false);
    cst_msg_rx_rate.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_msg_rx_rate.common().member_flags().IS_KEY(false);
    cst_msg_rx_rate.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_msg_rx_rate.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("double",
            false));


    cst_msg_rx_rate.detail().name("msg_rx_rate");

    type_object->complete().struct_type().member_seq().emplace_back(cst_msg_rx_rate);


    // Header
    type_object->complete().struct_type().header().detail().type_name("DdsTopicData");
    // TODO inheritance
    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("DdsTopicData", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("DdsTopicData", true);
}

const TypeIdentifier* GetDdsTopicIdentifier(
        bool complete)
{
    const TypeIdentifier* c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("DdsTopic", complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetDdsTopicObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("DdsTopic", complete);
}

const TypeObject* GetDdsTopicObject(
        bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopic", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteDdsTopicObject();
    }
    //else
    return GetMinimalDdsTopicObject();
}

const TypeObject* GetMinimalDdsTopicObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopic", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_name;
    mst_name.common().member_id(memberId++);
    mst_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_name.common().member_flags().IS_OPTIONAL(false);
    mst_name.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_name.common().member_flags().IS_KEY(false);
    mst_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    MD5 name_hash("name");
    for (int i = 0; i < 4; ++i)
    {
        mst_name.detail().name_hash()[i] = name_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_name);

    MinimalStructMember mst_type_name;
    mst_type_name.common().member_id(memberId++);
    mst_type_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_type_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_type_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_type_name.common().member_flags().IS_OPTIONAL(false);
    mst_type_name.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_type_name.common().member_flags().IS_KEY(false);
    mst_type_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_type_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    MD5 type_name_hash("type_name");
    for (int i = 0; i < 4; ++i)
    {
        mst_type_name.detail().name_hash()[i] = type_name_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_type_name);

    MinimalStructMember mst_type_discovered;
    mst_type_discovered.common().member_id(memberId++);
    mst_type_discovered.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_type_discovered.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_type_discovered.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_type_discovered.common().member_flags().IS_OPTIONAL(false);
    mst_type_discovered.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_type_discovered.common().member_flags().IS_KEY(false);
    mst_type_discovered.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_type_discovered.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    MD5 type_discovered_hash("type_discovered");
    for (int i = 0; i < 4; ++i)
    {
        mst_type_discovered.detail().name_hash()[i] = type_discovered_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_type_discovered);

    MinimalStructMember mst_type_mismatch;
    mst_type_mismatch.common().member_id(memberId++);
    mst_type_mismatch.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_type_mismatch.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_type_mismatch.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_type_mismatch.common().member_flags().IS_OPTIONAL(false);
    mst_type_mismatch.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_type_mismatch.common().member_flags().IS_KEY(false);
    mst_type_mismatch.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_type_mismatch.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    MD5 type_mismatch_hash("type_mismatch");
    for (int i = 0; i < 4; ++i)
    {
        mst_type_mismatch.detail().name_hash()[i] = type_mismatch_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_type_mismatch);

    MinimalStructMember mst_qos_mismatch;
    mst_qos_mismatch.common().member_id(memberId++);
    mst_qos_mismatch.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_qos_mismatch.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_qos_mismatch.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_qos_mismatch.common().member_flags().IS_OPTIONAL(false);
    mst_qos_mismatch.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_qos_mismatch.common().member_flags().IS_KEY(false);
    mst_qos_mismatch.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_qos_mismatch.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    MD5 qos_mismatch_hash("qos_mismatch");
    for (int i = 0; i < 4; ++i)
    {
        mst_qos_mismatch.detail().name_hash()[i] = qos_mismatch_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_qos_mismatch);

    MinimalStructMember mst_data;
    mst_data.common().member_id(memberId++);
    mst_data.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_data.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_data.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_data.common().member_flags().IS_OPTIONAL(false);
    mst_data.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_data.common().member_flags().IS_KEY(false);
    mst_data.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_data.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("DdsTopicData", 100,
            false));

    MD5 data_hash("data");
    for (int i = 0; i < 4; ++i)
    {
        mst_data.detail().name_hash()[i] = data_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_data);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("DdsTopic", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("DdsTopic", false);
}

const TypeObject* GetCompleteDdsTopicObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("DdsTopic", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_name;
    cst_name.common().member_id(memberId++);
    cst_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_name.common().member_flags().IS_OPTIONAL(false);
    cst_name.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_name.common().member_flags().IS_KEY(false);
    cst_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    cst_name.detail().name("name");

    type_object->complete().struct_type().member_seq().emplace_back(cst_name);

    CompleteStructMember cst_type_name;
    cst_type_name.common().member_id(memberId++);
    cst_type_name.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_type_name.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_type_name.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_type_name.common().member_flags().IS_OPTIONAL(false);
    cst_type_name.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_type_name.common().member_flags().IS_KEY(false);
    cst_type_name.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_type_name.common().member_type_id(*TypeObjectFactory::get_instance()->get_string_identifier(255, false));

    cst_type_name.detail().name("type_name");

    type_object->complete().struct_type().member_seq().emplace_back(cst_type_name);

    CompleteStructMember cst_type_discovered;
    cst_type_discovered.common().member_id(memberId++);
    cst_type_discovered.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_type_discovered.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_type_discovered.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_type_discovered.common().member_flags().IS_OPTIONAL(false);
    cst_type_discovered.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_type_discovered.common().member_flags().IS_KEY(false);
    cst_type_discovered.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_type_discovered.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    cst_type_discovered.detail().name("type_discovered");

    type_object->complete().struct_type().member_seq().emplace_back(cst_type_discovered);

    CompleteStructMember cst_type_mismatch;
    cst_type_mismatch.common().member_id(memberId++);
    cst_type_mismatch.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_type_mismatch.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_type_mismatch.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_type_mismatch.common().member_flags().IS_OPTIONAL(false);
    cst_type_mismatch.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_type_mismatch.common().member_flags().IS_KEY(false);
    cst_type_mismatch.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_type_mismatch.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    cst_type_mismatch.detail().name("type_mismatch");

    type_object->complete().struct_type().member_seq().emplace_back(cst_type_mismatch);

    CompleteStructMember cst_qos_mismatch;
    cst_qos_mismatch.common().member_id(memberId++);
    cst_qos_mismatch.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_qos_mismatch.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_qos_mismatch.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_qos_mismatch.common().member_flags().IS_OPTIONAL(false);
    cst_qos_mismatch.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_qos_mismatch.common().member_flags().IS_KEY(false);
    cst_qos_mismatch.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_qos_mismatch.common().member_type_id(*TypeObjectFactory::get_instance()->get_type_identifier("bool", false));


    cst_qos_mismatch.detail().name("qos_mismatch");

    type_object->complete().struct_type().member_seq().emplace_back(cst_qos_mismatch);

    CompleteStructMember cst_data;
    cst_data.common().member_id(memberId++);
    cst_data.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_data.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_data.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_data.common().member_flags().IS_OPTIONAL(false);
    cst_data.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_data.common().member_flags().IS_KEY(false);
    cst_data.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_data.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("DdsTopicData", 100,
            true));

    cst_data.detail().name("data");

    type_object->complete().struct_type().member_seq().emplace_back(cst_data);


    // Header
    type_object->complete().struct_type().header().detail().type_name("DdsTopic");
    // TODO inheritance
    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("DdsTopic", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("DdsTopic", true);
}

const TypeIdentifier* GetMonitoringTopicsIdentifier(
        bool complete)
{
    const TypeIdentifier* c_identifier = TypeObjectFactory::get_instance()->get_type_identifier("MonitoringTopics",
                    complete);
    if (c_identifier != nullptr && (!complete || c_identifier->_d() == EK_COMPLETE))
    {
        return c_identifier;
    }

    GetMonitoringTopicsObject(complete); // Generated inside
    return TypeObjectFactory::get_instance()->get_type_identifier("MonitoringTopics", complete);
}

const TypeObject* GetMonitoringTopicsObject(
        bool complete)
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("MonitoringTopics", complete);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }
    else if (complete)
    {
        return GetCompleteMonitoringTopicsObject();
    }
    //else
    return GetMinimalMonitoringTopicsObject();
}

const TypeObject* GetMinimalMonitoringTopicsObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("MonitoringTopics", false);
    if (c_type_object != nullptr)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_MINIMAL);
    type_object->minimal()._d(TK_STRUCTURE);

    type_object->minimal().struct_type().struct_flags().IS_FINAL(false);
    type_object->minimal().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->minimal().struct_type().struct_flags().IS_NESTED(false);
    type_object->minimal().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    MinimalStructMember mst_topics;
    mst_topics.common().member_id(memberId++);
    mst_topics.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    mst_topics.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    mst_topics.common().member_flags().IS_EXTERNAL(false); // Unsupported
    mst_topics.common().member_flags().IS_OPTIONAL(false);
    mst_topics.common().member_flags().IS_MUST_UNDERSTAND(false);
    mst_topics.common().member_flags().IS_KEY(false);
    mst_topics.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    mst_topics.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("DdsTopic", 100,
            false));

    MD5 topics_hash("topics");
    for (int i = 0; i < 4; ++i)
    {
        mst_topics.detail().name_hash()[i] = topics_hash.digest[i];
    }
    type_object->minimal().struct_type().member_seq().emplace_back(mst_topics);


    // Header
    // TODO Inheritance
    //type_object->minimal().struct_type().header().base_type()._d(EK_MINIMAL);
    //type_object->minimal().struct_type().header().base_type().equivalence_hash()[0..13];
    TypeIdentifier identifier;
    identifier._d(EK_MINIMAL);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->minimal().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("MonitoringTopics", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("MonitoringTopics", false);
}

const TypeObject* GetCompleteMonitoringTopicsObject()
{
    const TypeObject* c_type_object = TypeObjectFactory::get_instance()->get_type_object("MonitoringTopics", true);
    if (c_type_object != nullptr && c_type_object->_d() == EK_COMPLETE)
    {
        return c_type_object;
    }

    TypeObject* type_object = new TypeObject();
    type_object->_d(EK_COMPLETE);
    type_object->complete()._d(TK_STRUCTURE);

    type_object->complete().struct_type().struct_flags().IS_FINAL(false);
    type_object->complete().struct_type().struct_flags().IS_APPENDABLE(false);
    type_object->complete().struct_type().struct_flags().IS_MUTABLE(false);
    type_object->complete().struct_type().struct_flags().IS_NESTED(false);
    type_object->complete().struct_type().struct_flags().IS_AUTOID_HASH(false); // Unsupported

    MemberId memberId = 0;
    CompleteStructMember cst_topics;
    cst_topics.common().member_id(memberId++);
    cst_topics.common().member_flags().TRY_CONSTRUCT1(false); // Unsupported
    cst_topics.common().member_flags().TRY_CONSTRUCT2(false); // Unsupported
    cst_topics.common().member_flags().IS_EXTERNAL(false); // Unsupported
    cst_topics.common().member_flags().IS_OPTIONAL(false);
    cst_topics.common().member_flags().IS_MUST_UNDERSTAND(false);
    cst_topics.common().member_flags().IS_KEY(false);
    cst_topics.common().member_flags().IS_DEFAULT(false); // Doesn't apply
    cst_topics.common().member_type_id(*TypeObjectFactory::get_instance()->get_sequence_identifier("DdsTopic", 100,
            true));

    cst_topics.detail().name("topics");

    type_object->complete().struct_type().member_seq().emplace_back(cst_topics);


    // Header
    type_object->complete().struct_type().header().detail().type_name("MonitoringTopics");
    // TODO inheritance
    TypeIdentifier identifier;
    identifier._d(EK_COMPLETE);

    eprosima::fastcdr::CdrSizeCalculator calculator(eprosima::fastcdr::CdrVersion::XCDRv1);
    size_t current_alignment {0};
    SerializedPayload_t payload(static_cast<uint32_t>(
                calculator.calculate_serialized_size(type_object->complete().struct_type(), current_alignment) + 4));
    eprosima::fastcdr::FastBuffer fastbuffer((char*) payload.data, payload.max_size);
    // Fixed endian (Page 221, EquivalenceHash definition of Extensible and Dynamic Topic Types for DDS document)
    eprosima::fastcdr::Cdr ser(
        fastbuffer, eprosima::fastcdr::Cdr::LITTLE_ENDIANNESS,
        eprosima::fastcdr::CdrVersion::XCDRv1); // Object that serializes the data.
    payload.encapsulation = ser.endianness() == eprosima::fastcdr::Cdr::BIG_ENDIANNESS ? CDR_BE : CDR_LE;

    ser << *type_object;
#if FASTCDR_VERSION_MAJOR == 1
    payload.length = (uint32_t)ser.getSerializedDataLength(); //Get the serialized length
#else
    payload.length = (uint32_t)ser.get_serialized_data_length(); //Get the serialized length
#endif // FASTCDR_VERSION_MAJOR == 1
    MD5 objectHash;
    objectHash.update((char*)payload.data, payload.length);
    objectHash.finalize();
    for (int i = 0; i < 14; ++i)
    {
        identifier.equivalence_hash()[i] = objectHash.digest[i];
    }

    TypeObjectFactory::get_instance()->add_type_object("MonitoringTopics", &identifier, type_object);
    delete type_object;
    return TypeObjectFactory::get_instance()->get_type_object("MonitoringTopics", true);
}
